"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const grpc_1 = require("@chatie/grpc");
/**
 * Huan(202003):
 *  @chatie/GRPC proto gen TS does not generate the ENUM type with reverse mapping.
 *  So we need to do it by ourselves:
 *    1. define the EventTypeRev, and
 *    2. loop EventType to fill it.
 */
exports.EventTypeRev = {};
for (const key in grpc_1.EventType) {
    const val = grpc_1.EventType[key];
    exports.EventTypeRev[val] = key;
}
//# sourceMappingURL=event-type-rev.js.map